import { Component } from '@angular/core';

@Component({
  moduleId: module.id,
  selector: 'empty-component',
  template: `<p>This is empty component. How to remove from routes?</p>`
})
export class EmptyComponent {

  constructor() {
  }
}
